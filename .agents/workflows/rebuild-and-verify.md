---
description: Rebuild index.html from sections and verify in browser
---

// turbo-all

1. Rebuild `index.html` from section files:
```bash
bash build.sh
```

2. Open in browser to verify:
```bash
open index.html
```

3. Check that:
   - All sections render with correct styling
   - No console errors in the browser's DevTools
   - Interactive components work (carousel, video player, scene selector)
